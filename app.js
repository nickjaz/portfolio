'user strict'

// contructor function to add later projects

function Project(name, date, webPath, collabs, tools, about, gitPath){
  this.name = name;
  this.date = date;
  this.webPath = webPath;
  this.collabs = collabs;
  this.tools = tools;
  this.about = about;
  this.gitPath = gitPath;
}
